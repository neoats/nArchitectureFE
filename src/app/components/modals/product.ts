export class Product{
    productId:number
    categoryId: number
    productName:string
    unitPrice:number
    unitsInStock: number /* | undefined
  product:string
  constructor(productId:number, categoryId:number, productName:string, unitPrice:number, unitsInStock:number, product:string){
    if(!product){
        throw new Error("The 'product' field is required.");
    }

    this.productId = productId;
    this.categoryId = categoryId;
    this.productName = productName;
    this.unitPrice = unitPrice;
    this.unitsInStock = unitsInStock;
    this.product = product;
} */
}
